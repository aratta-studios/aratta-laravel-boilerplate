import {accountingDebtorCreditorTypes, accountingTableHeaders, accountingTypes} from "../data/constants";
import {
    accountingCreditorDebtorField, accountingTableData,
    accountingTypeField,
} from "../data/convertors";


const state = {
};

// getters
const getters = {};

// actions
const actions = {
};

// mutations
const mutations = {
};

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}
